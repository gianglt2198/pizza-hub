apiVersion: apps/v1
kind: Deployment
metadata:
  name: pizza-app
spec:
  template:
    spec:
      containers:
      - name: pizza-app
        resources:
          requests:  
            memory: "128Mi"  
            cpu: "100m"  
          limits:  
            memory: "256Mi"  
            cpu: "500m"  