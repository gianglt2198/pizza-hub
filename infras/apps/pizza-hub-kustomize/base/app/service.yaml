apiVersion: v1
kind: Service
metadata:
  name: pizza-app-svc
  namespace: pizza-hub
spec:
  selector:
    app: pizza-app
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort